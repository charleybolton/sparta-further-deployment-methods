apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-deployment-2
  labels:
    app: nodejs-2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nodejs-2
  template:
    metadata:
      labels:
        app: nodejs-2
    spec:
      containers:
      - name: nodejs
        image: chrleybolton/sparta-test-app:latest
        ports:
        - containerPort: 3000
        env:
        - name: DB_HOST
          value: "mongodb://mongodb-svc:27017/sparta"
        resources:
          requests:
            cpu: "100m"
          limits:
            cpu: "200m"

---
apiVersion: v1
kind: Service
metadata:
  name: nodejs-svc-2
spec:
  type: LoadBalancer
  ports:
  - port: 9000
    targetPort: 3000
    nodePort: 30002
  selector:
    app: nodejs-2

---

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: nodejs-scale-2
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: nodejs-deployment-2
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50